<h1>Good Vurger</h1>

<h3>Vegetarian Burgers that your carnivorous friends will love.</h3>

<ul>
	{{#each burgers}}
	<li>
		<p>ID: {{this.id}}</p>
		<p>Burger: {{this.burger}}</p>
	</li>
	{{/each}}
</ul>

<form action="/" method="POST">
	<textarea type="text" name="burger"></textarea>

	<button type="submit">Submit</button>
</form>